const migraitonHeaderOriginalText = "team.";

var migrationTextOptions = {
    strings: [migraitonHeaderOriginalText,'business.', 'startup.'
    ],
    typeSpeed: 50,
    backSpeed: 50,
    showCursor: true,
    smartBackspace: true,
    loop: true
  };
  
  var migrationTexttyped = new Typed('#typed', migrationTextOptions);